* add system user with empty password

* block login with empty password

* add Ask echo flag
** sharpl
** use to read passwords
** add password repeat/check on new admin

* add table BeforeInsert event
** add user.createdAt timestamp
*** add Timestamp column
**** DateTime

* add ForeignKey

* add Table.Update
** track stored values in tx

* ask for admin name & email if no user found on startup
* track insert/update status in cx/tx
* add Boolean column
* add Decimal column
** decimal
* model Feed

* add slog

sudo -u postgres psql [hostr]
alter user postgres with encrypted password ''
create user hostr
alter user hostr with encrypted password ''
create database hostr
grant all privileges on hostr to hostr
grant all on schema public to hostr